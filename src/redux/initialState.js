export const initialState = {
    cars: null,
    filter: null,
    
  };
  
  export const initialStateRoot = {
    isLoading: false,
    error: '',
  };

  export const initialStateFavorite = {
    favoriteCars: [],
  }